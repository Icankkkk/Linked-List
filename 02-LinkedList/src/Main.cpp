#include "List.h"

// main fungsi
int main(int argc, char const *argv[])
{
   
   // second = new Node();

    // head
  /*  head -> data = 20;
    head -> nama = "Faisal";
    head -> grade = 'A';
    head -> next = second;

    // second node
    second -> data = 30;
    second -> nama = "kevin";
    second -> grade = 'B';
    second -> next = nullptr; */

    // output
  /*  push_head(&head, 18, "fadia", 'C');
    push_head(&head, 17, "budi", 'A');
    push_head(&head, 20, "dimas", 'B'); */

 /*   push_head(&head);
    head -> next = nullptr;
    print_list(head);
    push_head(&head);
    cout << "Setelah di tambah\n";
    print_list(head); */

    switch_case();  // memanggil fungsi

    return 0;
}

/*
    link refrensi : https://www.bitdegree.org%2Flearn%2Flinked-list-c-plus-plus
*/